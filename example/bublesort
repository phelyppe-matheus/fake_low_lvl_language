load 0
addi 10
store 91  // carrega o conteúdo 10 na posição 1 do vetor
load 0
addi 3
store 92  // carrega o conteúdo 3  na posição 2 do vetor
load 0
addi 6
store 93  // carrega o conteúdo 6  na posição 3 do vetor
load 0
addi 8
store 94  // carrega o conteudo 8  na posição 4 do vetor
load 0
addi 1
store 95  // carrega o conteúdo 1  na posição 5 do vetor
load 0
addi 1
store 96  // carrega o conteúdo 1  na variavel 'i'
subi 4
load 96 // inicio do fim externo
addi 1
jpos 'fim do para externo'
    store 97
    load 97 // inicio do fim interno
    subi 5
    jpos 'fim do para interno'
        load 96
        subi 1
            jpos 'proximo if'
                load  91
                jump  'fim do switch'
            subi  1
            jpos 'proximo if'
                load  92
                jump  'fim do switch'
            subi  1
            jpos 'proximo if'
                load  93
                jump  'fim do switch'
            subi  1
            jpos 'proximo if'
                load  94
                jump  'fim do switch'
            subi  1
            jpos 'proximo if'
                load  95
                jump  'fim do switch'
        store 98  // A[i]
        load 97
        subi 1
            jpos 'proximo if'
                load  91
                jump  'fim do switch'
            subi  1
            jpos 'proximo if'
                load  92
                jump  'fim do switch'
            subi  1
            jpos 'proximo if'
                load  93
                jump  'fim do switch'
            subi  1
            jpos 'proximo if'
                load  94
                jump  'fim do switch'
            subi  1
            jpos 'proximo if'
                load  95
                jump  'fim do switch'
        store 99 // A[j]
            load 99
            sub  98
            jpos 'fim do if'
                load 96
                subi 1
                    jpos 'proximo if'
                        load 99
                        store  91
                        jump  'fim do switch'
                    subi  1
                    jpos 'proximo if'
                        load 99
                        store  92
                        jump  'fim do switch'
                    subi  1
                    jpos 'proximo if'
                        load 99
                        store  93
                        jump  'fim do switch'
                    subi  1
                    jpos 'proximo if'
                        load 99
                        store  94
                        jump  'fim do switch'
                    subi  1
                    jpos 'proximo if'
                        load 99
                        store  95
                        jump  'fim do switch'
                load 97
                subi 1
                    jpos 'proximo if'
                        load  98
                        store 91
                        jump  'fim do switch'
                    subi  1
                    jpos 'proximo if'
                        load  98
                        store 92
                        jump  'fim do switch'
                    subi  1
                    jpos 'proximo if'
                        load  98
                        store 93
                        jump  'fim do switch'
                    subi  1
                    jpos 'proximo if'
                        load  98
                        store 94
                        jump  'fim do switch'
                    subi  1
                    jpos 'proximo if'
                        load  98
                        store 95
                        jump  'fim do switch'
    load 97
    addi 1
    store 97 // decrementa variável 'j'
    jump 'inicio do fim interno'
load 96
addi 1
store 96 // decrementa variável 'i'
jump 'inicio do fim externo'
'fim do para interno'
'fim do para externo'
